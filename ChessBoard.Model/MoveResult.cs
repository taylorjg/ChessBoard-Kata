namespace Chess.Model
{
    /// <summary>
    /// Specifies the possible outcomes of an attempted move.
    /// </summary>
    public enum MoveResult
    {
        Valid,
        Illegal,
        Collision
    }
}
